import { registerFeature } from '../../core/navigation.registry';

registerFeature({
  id: 'dashboard',
  name: 'Inicio',
  navigation: [{ label: 'Inicio', path: '/dashboard', icon: 'IN' }],
  routes: [],
});
